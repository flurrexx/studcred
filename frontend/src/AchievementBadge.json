[
  "function totalIssued() view returns (uint256)",
  "function issuers(address) view returns (bool)",
  "function addIssuer(address issuer)",
  "function removeIssuer(address issuer)",
  "function issueBadge(address student, string uri, string category) returns (uint256)",
  "function revokeBadge(uint256 tokenId)",
  "function verifyBadge(uint256 tokenId) view returns (bool valid, address owner_, address issuer, uint256 issuedAt, string category, string uri)",
  "function ownerOf(uint256 tokenId) view returns (address)",
  "function tokenURI(uint256 tokenId) view returns (string)",
  "function badgeInfo(uint256) view returns (address issuer, uint256 issuedAt, string category)",
  "event BadgeIssued(uint256 indexed tokenId, address indexed student, address indexed issuer, string category, string tokenURI)",
  "event BadgeRevoked(uint256 indexed tokenId)",
  "event IssuerAdded(address indexed issuer)",
  "event IssuerRemoved(address indexed issuer)"
]
