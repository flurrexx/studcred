{
  "name": "studcred-contracts",
  "version": "1.0.0",
  "description": "StudCred — NFT-бейджи достижений студентов. Смарт-контракты и скрипты деплоя.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy",
    "issue:local": "hardhat run scripts/issueBadge.js --network localhost"
  },
  "keywords": ["nft", "erc721", "soulbound", "credentials", "education"],
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.0"
  }
}
